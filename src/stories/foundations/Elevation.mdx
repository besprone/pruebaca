import { Meta } from '@storybook/blocks';
import elevationFigmaDoc from './doc/elevation-from-figma.md?raw';
import { FigmaDoc } from './FigmaDoc';
import { ElevationScale } from './ElevationScale';

<Meta title="Foundations/Elevation" />

# Elevation

Escala de sombras/elevación alineada con `kubo.elevation` en Figma.

```bash
npm run tokens:elevation
# o
npm run tokens:all
```

## Notas de Figma

<FigmaDoc content={elevationFigmaDoc} />

<ElevationScale />
