import { Meta } from '@storybook/blocks';
import radiusFigmaDoc from './doc/radius-from-figma.md?raw';
import { FigmaDoc } from './FigmaDoc';
import { RadiusScale } from './RadiusScale';

<Meta title="Foundations/Radius" />

# Radius

Escala de border radius alineada con `kubo.radius` de Figma, para App y Web.

```bash
npm run tokens:radius
# o
npm run tokens:all
```

## Notas de Figma

<FigmaDoc content={radiusFigmaDoc} />

<RadiusScale />
