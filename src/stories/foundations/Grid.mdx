import { Meta } from '@storybook/blocks';
import gridFigmaDoc from './doc/grid-from-figma.md?raw';
import { FigmaDoc } from './FigmaDoc';
import { GridScale } from './GridScale';

<Meta title="Structure/Grid" />

# Grid

Sistema de grid responsive alineado con `kubo.grid` en Figma para App y Web.

```bash
npm run tokens:grid
# o
npm run tokens:all
```

## Notas de Figma

<FigmaDoc content={gridFigmaDoc} />

<GridScale />
